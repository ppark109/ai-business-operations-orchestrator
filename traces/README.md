# Traces

Local trace exports may contain case details, model inputs, and reviewer
comments, so raw trace files are ignored by default.

Commit only curated, scrubbed fixtures when they are useful for tests or docs.
