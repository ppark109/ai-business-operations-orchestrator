# Data

This directory separates curated inputs from local runtime artifacts.

- `seed/`: synthetic demo cases and sample intake artifacts.
- `held_out/`: cases reserved for evals and regression tests.
- `runtime/`: local databases and generated state ignored by Git.
